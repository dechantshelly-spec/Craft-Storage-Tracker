# Netlify config for a single-page app (Vite + React)
[build]
  command = "npm run build"
  publish = "dist"

# Send all routes back to index.html so the app boots (prevents 404/blank)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
